export enum ErrorCodes {
    CATEGORY020 = 'CATEGORY.020',
}

export enum ErrorMessages {
    CATEGORY020 = 'Article category with this title already exists.',
    ARTICLE020 = 'Article with this title already exists.',
    TAG020 = 'Tag with this slug already exists.',

    USER409000 = 'Email already taken',
}
